{
  "name": "reto-pokemon-microfrontends",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm --filter \"./apps/*\" build",
    "dev": "pnpm build && node scripts/dev.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}